# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build

# zip
/zip

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.history

# secrets
secrets.*.js

/venv

/chrome-boiler.zip

# env secrets (any location)
.env
**/.env
!.env.example

# agent runtime data
agent/data/

# internal planning / design docs — keep local, never push.
# All design specs live under docs/ (docs/specs/), so ignore the whole tree.
BUG_BOUNTY_AUTOMATION_PLAN.md
docs/

# generated tool output (graphify) — regenerated, not source
src/graphify-out/

# local Claude Code / superpowers tooling — machine-local, never push
.superpowers/
.claude/settings.local.json